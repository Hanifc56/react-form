/**
 *1. Use form Tag and onSubmit handler with event(e)
 * From e access e.target.[name of the input field.value]
 *
 * 2. Use indivitual state  and use OnChange{} event for get the in input value.
 * set initil value of state and can use to validate ypur data.
 * You can also set requred input type for some fields.
 *
 * 3. Controlled element: one object in a state holding all the input fields value
 *
 * 4. uncontrolled element: UseRef()hook to create a reference to the element and access value by using like: nameRef.current.value
 *
 * 5. Use hook to handle state and submit and error example react hook form
 *
 * 6. Reuseable Component.
 *
 */
